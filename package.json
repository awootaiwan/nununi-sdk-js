{
  "name": "cupid-sdk-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html --port 3000",
    "demo": "parcel demo/index.html --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html --out-dir dist",
    "build:app": "parcel build src/app.js --out-dir demo/js",
    "build:no-cache": "parcel build src/index.html --no-cache --out-dir dist",
    "build:no-minify": "parcel build src/index.html --no-minify --out-dir dist"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^9.6.0",
    "axios": "^0.18.0",
    "querystring": "^0.2.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/plugin-transform-async-to-generator": "^7.4.4",
    "@babel/plugin-transform-runtime": "^7.4.4",
    "@babel/preset-env": "^7.4.5",
    "@babel/preset-react": "^7.0.0",
    "babel-loader": "^8.0.6",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.21.0"
  }
}
